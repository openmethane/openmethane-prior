Removed the deprecated `OCH4_TOTAL` and `LANDMASK` variables from the prior emissions output. These were deprecated in favour of `ch4_total` and `land_mask` respectively, which carry the same values.
