The `land_mask` variable in the prior emissions output is now stored as an 8-bit integer instead of a 64-bit integer, since it only ever holds the values 0 and 1. The values it reports are unchanged.
