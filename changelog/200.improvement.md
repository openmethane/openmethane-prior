Extracted shared `filter_by_anzsic_code_family()` helper to remove duplicated ANZSIC code family filtering in NPI, Safeguard, and oil/gas site sources. Fixed `simplify_anzsic_code()` so subdivision codes ending in zero (e.g. "30") are not over-shortened.
